package org.monash.nimrod.optim;

import org.junit.Test;
import java.util.Properties;

public class SimplexTest {

	private static final String DEFAULT_PARAMSTRING = "parameter fred label \"fff\" float range from 1 to 22 step 4\n parameter jim float range from 0 to 50";
	
	@Test
	public void fuck() throws Exception {
		Properties props = new Properties();
		props.setProperty("kek", "fuck");
		props.setProperty("lel", "shit");
		
		props.store(System.out, null);
		
		int x = 0;
	}

}
